<table>
  <tr>
    <th>Project</th>
    <th>Task</th>
    <th>Item</th>
    <th>Cost</th>
    <th>Date</th>
    <th>State</th>
  </tr>

<% @expenses.each do |expense| %>
  <tr>
    <td><%=h expense.task.project %></td>
    <td><%=h expense.task %></td>
    <td><%=h expense.name %></td>
    <td><%=h expense.cost_to_s %></td>
    <td><%=h expense.date %></td>
    <td><%=h expense.state_to_s %></td>
    <td><%= link_to 'Show', expense %> |
    <%= link_to 'Edit', edit_expense_path(expense) %> |
    <%= link_to 'Destroy', expense, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New expense', new_expense_path %>
